{
  "name": "repanel",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:api": "pnpm --filter @repanel/api dev",
    "dev:web": "pnpm --filter @repanel/web dev",
    "dev:runtime": "pnpm --filter @repanel/runtime dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
